name: Legacy Aesolator APK lane (disabled)

on:
  workflow_dispatch:

jobs:
  lane_relocated:
    runs-on: ubuntu-24.04
    steps:
      - name: Lane moved to WCP Archive
        run: |
          echo "Aesolator APK lane moved to kosoymiki/wcp-runtime-lanes"
          echo "Use workflow: .github/workflows/ci-aesolator-apk.yml"
